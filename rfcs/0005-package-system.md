# 0005 - Package System

- Status: draft (Milestone 4)
- Related: `docs/06-compiler.md`, `docs/09-roadmap.md`

`pickle.toml` manifests, `src/*.pkl` discovery, module/import resolution,
dependencies, frozen lockfile, and the `pickle package`/`publish` surface.