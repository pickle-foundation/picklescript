[package]
name = "pickle-cli"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
pickle-compiler = { path = "../compiler" }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
serde = { version = "1", features = ["derive"] }
toml = { version = "0.8", features = ["parse"] }
rustyline = "16"